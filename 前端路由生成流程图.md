# 前端路由生成详细流程图

## 一、整体架构概览

```mermaid
graph TB
    A[Vite 启动] --> B[加载 @elegant-router/vue 插件]
    B --> C[自动生成 routes.js 和 imports.js]
    C --> D[创建 Vue Router 实例]
    D --> E[注册路由守卫]
    E --> F[应用启动]
    F --> G{路由模式判断}
    G -->|static 静态模式| H[从本地 generatedRoutes 加载]
    G -->|dynamic 动态模式| I[从后端 API 获取]
    H --> J[转换为 Vue Router 路由]
    I --> J
    J --> K[动态添加到 Vue Router]
    K --> L[生成全局菜单]
    L --> M[路由可用]
```

## 二、详细流程分解

### 阶段 1：构建时路由自动生成（@elegant-router/vue 插件）

```mermaid
graph TD
    A[Vite 启动/热更新] --> B[elegant-router-vue 插件执行]
    B --> C[扫描 src/views 目录]
    C --> D[解析文件目录结构]
    D --> E[根据文件命名规则生成路由配置]
    E --> F[生成 src/router/elegant/routes.js]
    E --> G[生成 src/router/elegant/imports.js]
    E --> H[生成 src/router/elegant/transform.js]

    F --> I[包含 generatedRoutes 数组]
    G --> J[包含 layouts 和 views 映射]
    H --> K[包含路由转换函数和名称路径映射]

    style B fill:#e1f5ff
    style F fill:#fff3e0
    style G fill:#fff3e0
    style H fill:#fff3e0
```

**生成的文件说明：**

- **routes.js**: 包含 `generatedRoutes` 数组，定义了所有自动生成的路由配置
  - 路由名称（name）
  - 路由路径（path）
  - 组件路径（component）：格式为 `layout.xxx$view.yyy` 或 `view.yyy`
  - 元信息（meta）：标题、图标、排序、权限角色等

- **imports.js**: 组件懒加载映射
  - `layouts`: 布局组件映射（base、blank）
  - `views`: 页面组件的懒加载函数映射

- **transform.js**: 路由转换工具
  - `transformElegantRoutesToVueRoutes`: 将 elegant 路由转换为 Vue Router 路由
  - `routeMap`: 路由名称与路径的双向映射表

### 阶段 2：Vue Router 初始化

```mermaid
graph TD
    A[应用启动 - main.js] --> B[调用 setupRouter]
    B --> C[创建 Vue Router 实例]
    C --> D[调用 createBuiltinVueRoutes]
    D --> E[创建内置路由：root 和 not-found]
    E --> F[转换为 Vue Router 路由格式]
    F --> G[初始化 router 实例]
    G --> H[调用 createRouterGuard]
    H --> I[注册进度条守卫]
    H --> J[注册路由守卫 beforeEach]
    H --> K[注册文档标题守卫]
    I --> L[router.isReady 等待路由就绪]
    J --> L
    K --> L

    style C fill:#e1f5ff
    style E fill:#fff3e0
    style J fill:#ffe1e1
```

**内置路由（builtin.js）：**

```javascript
ROOT_ROUTE = {
  name: 'root',
  path: '/',
  redirect: '/home',  // 由 VITE_ROUTE_HOME 环境变量决定
  meta: { title: 'root', constant: true }
}

NOT_FOUND_ROUTE = {
  name: 'not-found',
  path: '/:pathMatch(.*)*',
  component: 'layout.blank$view.404',
  meta: { title: 'not-found', constant: true }
}
```

### 阶段 3：路由守卫初始化（应用启动时）

```mermaid
graph TD
    A[用户访问应用] --> B[触发 router.beforeEach]
    B --> C[调用 initRoute]
    C --> D{常量路由是否已初始化?}
    D -->|否| E[调用 routeStore.initConstantRoute]
    D -->|是| F{是否为 not-found 路由?}

    E --> E1{路由模式判断}
    E1 -->|static| E2[调用 createStaticRoutes]
    E1 -->|dynamic| E3[调用 fetchGetConstantRoutes API]

    E2 --> E4[分离 constantRoutes 和 authRoutes]
    E3 --> E4

    E4 --> E5[调用 addAuthRoutes 存储到 Pinia]
    E5 --> E6[调用 handleAuthRoutes]
    E6 --> E7[转换为 Vue 路由]
    E7 --> E8[通过 router.addRoute 动态添加]
    E8 --> E9[生成全局菜单]
    E9 --> E10[设置 isInitConstantRoute = true]

    E10 --> F
    F -->|是| G[重定向回原始路径]
    F -->|否| H[允许访问常量路由]

    style E fill:#e1f5ff
    style E2 fill:#fff3e0
    style E3 fill:#e8f5e9
    style E8 fill:#ffe1e1
```

### 阶段 4：权限路由初始化（登录后）

```mermaid
graph TD
    A[用户登录成功] --> B[访问需要权限的路由]
    B --> C[触发 router.beforeEach]
    C --> D{权限路由是否已初始化?}
    D -->|否| E[调用 routeStore.initAuthRoute]
    D -->|是| F[检查权限]

    E --> G{路由模式判断}
    G -->|static 静态模式| H[调用 initStaticAuthRoute]
    G -->|dynamic 动态模式| I[调用 initDynamicAuthRoute]

    H --> H1[调用 createStaticRoutes 获取所有 authRoutes]
    H1 --> H2{是否为超级管理员?}
    H2 -->|是| H3[添加所有权限路由]
    H2 -->|否| H4[根据用户角色过滤路由 filterAuthRoutesByRoles]
    H4 --> H5[调用 addAuthRoutes]

    I --> I1[调用 fetchGetUserRoutes API]
    I1 --> I2[从后端获取用户可访问的路由树]
    I2 --> I3[调用 addAuthRoutes]
    I3 --> I4[设置首页路由 setRouteHome]
    I4 --> I5[更新根路由重定向]

    H5 --> J[调用 handleAuthRoutes]
    I5 --> J

    J --> J1[按 order 排序路由 sortRoutesByOrder]
    J1 --> J2[转换为 Vue Router 路由 getAuthVueRoutes]
    J2 --> J3[重置旧的 Vue 路由 resetVueRoutes]
    J3 --> J4[动态添加到 Vue Router addRoutesToVueRouter]
    J4 --> J5[生成全局菜单 getGlobalMenus]
    J5 --> J6[计算缓存路由 getCacheRoutes]
    J6 --> J7[设置 isInitAuthRoute = true]

    J7 --> F
    F --> G1[检查用户角色与路由 roles 匹配]
    G1 --> G2{有权限?}
    G2 -->|是| G3[允许访问]
    G2 -->|否| G4[跳转到 403 页面]

    style H fill:#fff3e0
    style I fill:#e8f5e9
    style J fill:#ffe1e1
    style J4 fill:#ff5252
```

### 阶段 5：路由转换详细流程（transformElegantRoutesToVueRoutes）

```mermaid
graph TD
    A[输入: elegant 路由数组] --> B[遍历每个路由]
    B --> C[调用 transformElegantRouteToVueRoute]
    C --> D{是否为单级路由?}

    D -->|是| E[拆分为 layout + view]
    E --> F[创建带 children 的路由结构]
    F --> F1[父路由: path + layout 组件]
    F1 --> F2[子路由: path='' + view 组件]
    F2 --> G[返回路由数组]

    D -->|否| H{component 类型?}
    H -->|layout.xxx| I[从 layouts 映射获取组件]
    H -->|view.xxx| J[从 views 映射获取组件]
    H -->|layout.xxx$view.yyy| K[单级路由特殊处理]

    I --> L{有子路由?}
    J --> L
    L -->|是| M[递归转换子路由]
    L -->|否| N[构建 vueRoute 对象]

    M --> O{是否为一级路由?}
    O -->|是| P[子路由放入 children]
    O -->|否| Q[子路由扁平化]

    P --> N
    Q --> N

    N --> R{未配置 redirect 且有子路由?}
    R -->|是| S[自动重定向到第一个子路由]
    R -->|否| T[返回 vueRoutes]

    S --> T
    G --> U[flatMap 合并所有路由]
    T --> U
    U --> V[输出: Vue Router 路由数组]

    style A fill:#e1f5ff
    style V fill:#4caf50
    style E fill:#fff3e0
    style M fill:#ffe1e1
```

**路由转换示例：**

```javascript
// 输入 - Elegant 路由（单级路由）
{
  name: 'home',
  path: '/home',
  component: 'layout.base$view.home',
  meta: { title: '首页', order: 1 }
}

// 输出 - Vue Router 路由
{
  path: '/home',
  component: BaseLayout,  // 从 layouts['base'] 获取
  children: [
    {
      name: 'home',
      path: '',
      component: () => import('@/views/home/index.vue'),  // 从 views['home'] 获取
      meta: { title: '首页', order: 1 }
    }
  ]
}
```

### 阶段 6：路由守卫完整判断流程

```mermaid
graph TD
    A[用户访问路由] --> B[beforeEach 守卫触发]
    B --> C[initRoute 初始化检查]
    C --> D{常量路由未初始化?}
    D -->|是| E[初始化常量路由]
    D -->|否| F{权限路由未初始化?}

    E --> G[重定向回原始路径]

    F -->|是| H{已登录?}
    F -->|否| I{是否为常量路由?}

    H -->|否| J[跳转到登录页]
    H -->|是| K[初始化权限路由]
    K --> G

    I -->|是| L[允许访问]
    I -->|否| M{是否为 not-found?}

    M -->|是| N{路由已初始化?}
    M -->|否| O[权限检查]

    N -->|是| P[检查路由是否存在]
    P --> Q{存在?}
    Q -->|是| R[跳转 403 无权限]
    Q -->|否| S[允许访问]

    O --> T[检查用户角色匹配]
    T --> U{有权限?}
    U -->|是| V[允许访问 handleRouteSwitch]
    U -->|否| W[跳转 403]

    V --> X{是否为外链?}
    X -->|是| Y[新窗口打开外链]
    Y --> Z[返回原页面]
    X -->|否| AA[正常导航]

    style A fill:#e1f5ff
    style E fill:#fff3e0
    style K fill:#fff3e0
    style J fill:#ff5252
    style W fill:#ff5252
    style R fill:#ff5252
    style V fill:#4caf50
    style L fill:#4caf50
    style AA fill:#4caf50
```

### 阶段 7：静态路由模式下的路由收集

```mermaid
graph TD
    A[调用 createStaticRoutes] --> B[合并 customRoutes + generatedRoutes]
    B --> C[遍历所有路由]
    C --> D{meta.constant === true?}
    D -->|是| E[推入 constantRoutes]
    D -->|否| F[推入 authRoutes]

    E --> G[返回常量路由数组]
    F --> H[返回权限路由数组]

    G --> I[常量路由包括:]
    I --> I1[403, 404, 500 错误页]
    I --> I2[login 登录页]

    H --> J[权限路由包括:]
    J --> J1[home 首页]
    J --> J2[about 关于]
    J --> J3[function 功能页]
    J --> J4[manage 系统管理]
    J --> J5[log 日志管理]
    J --> J6[multi-menu 多级菜单]
    J --> J7[user-center 个人中心]
    J --> J8[exception 异常页集合]

    style A fill:#e1f5ff
    style B fill:#fff3e0
    style G fill:#4caf50
    style H fill:#ff9800
```

### 阶段 8：动态路由模式下的后端交互

```mermaid
sequenceDiagram
    participant U as 用户
    participant F as 前端 RouteStore
    participant A as 后端 API
    participant D as 数据库
    participant M as 菜单树构建

    U->>F: 登录后访问权限路由
    F->>A: GET /api/route/getUserRoutes
    A->>A: 验证 JWT Token
    A->>D: 查询用户角色 ID
    D-->>A: 返回角色 ID 列表
    A->>D: 根据角色查询菜单权限
    D-->>A: 返回扁平菜单列表
    A->>M: buildMenuTree 构建树形结构
    M-->>A: 返回路由树
    A-->>F: {routes: [...], home: 'home'}
    F->>F: addAuthRoutes 存储路由
    F->>F: handleAuthRoutes 处理路由
    F->>F: 转换为 Vue Router 格式
    F->>F: router.addRoute 动态注册
    F->>F: 生成全局菜单
    F-->>U: 路由可用，渲染页面
```

### 阶段 9：路由注册到 Vue Router 的完整流程

```mermaid
graph TD
    A[handleAuthRoutes 调用] --> B[排序路由 sortRoutesByOrder]
    B --> C[转换为 Vue 路由 getAuthVueRoutes]
    C --> D[调用 resetVueRoutes]
    D --> E[遍历 removeRouteFns]
    E --> F[调用每个 removeFn 移除旧路由]
    F --> G[清空 removeRouteFns 数组]

    G --> H[调用 addRoutesToVueRouter]
    H --> I[遍历 Vue 路由数组]
    I --> J[调用 router.addRoute]
    J --> K[获取 removeFn 移除函数]
    K --> L[保存 removeFn 到 removeRouteFns]
    L --> M{还有路由?}
    M -->|是| I
    M -->|否| N[所有路由注册完成]

    N --> O[调用 getGlobalMenus]
    O --> P[递归遍历路由树]
    P --> Q{hideInMenu === true?}
    Q -->|是| R[跳过该路由]
    Q -->|否| S[创建菜单项]
    S --> T{有可见子路由?}
    T -->|是| U[递归创建子菜单]
    T -->|否| V[添加菜单项]
    U --> V
    R --> W{还有路由?}
    V --> W
    W -->|是| P
    W -->|否| X[菜单生成完成]

    X --> Y[调用 getCacheRoutes]
    Y --> Z[收集 keepAlive 路由]
    Z --> AA[缓存路由名称列表]

    style A fill:#e1f5ff
    style D fill:#ffe1e1
    style J fill:#ff9800
    style N fill:#4caf50
    style X fill:#4caf50
    style AA fill:#2196f3
```

## 三、关键配置说明

### 环境变量配置

```bash
# .env 文件
VITE_AUTH_ROUTE_MODE=static          # 路由模式：static（静态）或 dynamic（动态）
VITE_ROUTE_HOME=home                 # 首页路由名称
VITE_ROUTER_HISTORY_MODE=history     # 路由历史模式：history 或 hash
VITE_BASE_URL=/                      # 应用基础路径
```

### 路由模式对比

| 特性 | static 静态模式 | dynamic 动态模式 |
|------|----------------|-----------------|
| 路由来源 | 本地 generatedRoutes | 后端 API 返回 |
| 适用环境 | 开发环境 | 生产环境 |
| 权限控制 | 前端根据 roles 过滤 | 后端根据用户角色返回 |
| 灵活性 | 较低 | 高 |
| 安全性 | 较低（路由配置暴露） | 高（后端控制） |

### 路由命名规范

```
一级路由: home, about, manage
二级路由: manage_user, manage_role, manage_menu
三级路由: multi-menu_first_child, function_hide-child_one
```

### 组件路径格式

```
单级路由: layout.base$view.home        → 展开为 layout + 默认子路由
带布局路由: layout.base                 → 使用 base 布局
视图路由: view.home                     → 直接使用 view 组件
```

## 四、核心函数调用链

### 1. 应用启动流程

```
main.js
  └─ setupRouter(app)
      ├─ createRouter({ routes: createBuiltinVueRoutes() })
      │   └─ transformElegantRoutesToVueRoutes([ROOT_ROUTE, NOT_FOUND_ROUTE])
      ├─ createRouterGuard(router)
      │   ├─ createProgressGuard
      │   ├─ createRouteGuard (beforeEach)
      │   └─ createDocumentTitleGuard
      └─ router.isReady()
```

### 2. 常量路由初始化流程

```
routeStore.initConstantRoute()
  └─ createStaticRoutes() 或 fetchGetConstantRoutes()
      └─ addAuthRoutes(constantRoutes)
          └─ handleAuthRoutes()
              ├─ sortRoutesByOrder(authRoutes)
              ├─ getAuthVueRoutes(sortRoutes)
              │   └─ transformElegantRoutesToVueRoutes(routes, layouts, views)
              │       └─ transformElegantRouteToVueRoute (递归)
              ├─ resetVueRoutes()
              ├─ addRoutesToVueRouter(vueRoutes)
              │   └─ router.addRoute(route) → removeFn
              ├─ getGlobalMenus(sortRoutes)
              └─ getCacheRoutes(vueRoutes)
```

### 3. 权限路由初始化流程

```
routeStore.initAuthRoute()
  ├─ initStaticAuthRoute()
  │   └─ createStaticRoutes()
  │       └─ filterAuthRoutesByRoles(routes, userInfo.roles)  // 非超级管理员
  │           └─ addAuthRoutes(filteredRoutes)
  │               └─ handleAuthRoutes()
  └─ initDynamicAuthRoute()
      └─ fetchGetUserRoutes()
          └─ addAuthRoutes(routes)
              └─ handleAuthRoutes()
```

### 4. 路由守卫判断流程

```
router.beforeEach(to, from, next)
  └─ initRoute(to)
      ├─ 初始化常量路由（如果未初始化）
      ├─ 初始化权限路由（如果未初始化且已登录）
      └─ 检查 not-found 路由
  └─ 权限检查
      ├─ 已登录访问登录页 → 跳转首页
      ├─ 常量路由 → 允许访问
      ├─ 未登录访问权限路由 → 跳转登录页
      ├─ 已登录且有权限 → 允许访问
      └─ 已登录无权限 → 跳转 403
```

## 五、特殊场景处理

### 1. 单级路由展开

```javascript
// 配置
{
  name: 'home',
  path: '/home',
  component: 'layout.base$view.home'
}

// 转换为
{
  path: '/home',
  component: BaseLayout,
  children: [{
    name: 'home',
    path: '',
    component: HomeView
  }]
}
```

### 2. 自动重定向

```javascript
// 未配置 redirect 的父路由
{
  name: 'manage',
  path: '/manage',
  component: 'layout.base',
  children: [
    { name: 'manage_user', path: '/manage/user' },
    { name: 'manage_role', path: '/manage/role' }
  ]
}

// 自动添加 redirect
{
  name: 'manage',
  path: '/manage',
  redirect: { name: 'manage_user' },  // 自动重定向到第一个子路由
  children: [...]
}
```

### 3. 路由缓存（keepAlive）

```javascript
// 配置 keepAlive
{
  name: 'manage_menu',
  path: '/manage/menu',
  component: 'view.manage_menu',
  meta: {
    title: '菜单管理',
    keepAlive: true  // 启用缓存
  }
}

// 收集到 cacheRoutes 数组
cacheRoutes = ['manage_menu']
```

### 4. 动态路由参数

```javascript
// 自动启用 props 透传
{
  name: 'manage_user-detail',
  path: '/manage/user-detail/:id',
  component: 'view.manage_user-detail',
  props: true  // 自动添加（如果包含 : 参数）
}
```

## 六、总结

前端路由生成的核心流程可以概括为以下几个阶段：

1. **构建时生成**：`@elegant-router/vue` 插件扫描 views 目录，自动生成路由配置
2. **Router 初始化**：创建 Vue Router 实例，注册内置路由（root、not-found）
3. **常量路由初始化**：应用启动时加载不需要权限的路由（登录页、错误页等）
4. **权限路由初始化**：登录后根据路由模式加载权限路由（静态过滤或动态获取）
5. **路由转换**：将 elegant 格式转换为 Vue Router 格式（处理布局、组件、子路由等）
6. **动态注册**：通过 `router.addRoute()` 动态添加路由到 Vue Router
7. **菜单生成**：根据权限路由生成全局菜单树
8. **守卫控制**：路由守卫负责权限验证、登录检查、路由初始化等

整个系统支持两种路由模式：
- **static 模式**：适合开发环境，路由配置在前端，根据角色过滤
- **dynamic 模式**：适合生产环境，路由由后端返回，更安全灵活
