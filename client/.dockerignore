# 依赖与构建产物（镜像内重新安装/构建）
node_modules
dist

# 版本控制与 IDE
.git
.gitignore
.idea
.vscode
.qoder
.trae

# 环境配置（构建时按需 COPY，其余不入镜像）
.env
.env.prod

# 锁文件（仅保留 pnpm-lock.yaml）
package-lock.json
yarn.lock

# 文档与临时文件
*.md
*.log

# 构建配置（已显式 COPY 需要的文件，其余忽略）
.eslintignore
.editorconfig
